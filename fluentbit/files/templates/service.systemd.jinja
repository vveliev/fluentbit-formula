[Unit]
Description={{ fluentbit.package.name }}
Requires=network.target
After=network.target

[Service]
Type=simple
ExecStart={{ fluentbit.package.archive }}{{ fluentbit.package.name }} -c {{ fluentbit.config.file }}
Restart=always

[Install]
WantedBy=multi-user.target
