[Unit]
Description={{ fluentbit.pkg.name }}
Requires=network.target
After=network.target

[Service]
Type=simple
ExecStart={{ fluentbit.pkg.archive }}{{ fluentbit.pkg.name }} -c {{ fluentbit.config.path }}/{{ fluentbit.config.filename }}
Restart=always

[Install]
WantedBy=multi-user.target
