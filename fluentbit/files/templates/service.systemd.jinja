[Unit]
Description={{ fluentbit.package.name }}
Requires=network.target
After=network.target

[Service]
Type=simple
ExecStart=/opt/fluent-bit/bin/{{ fluentbit.package.name }} -c {{ fluentbit.config.file }}
Restart=always

[Install]
WantedBy=multi-user.target
